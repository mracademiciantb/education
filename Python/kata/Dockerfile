# Використовуємо офіційний образ Python як базовий
FROM python:3.8-slim

# Встановлюємо робочу директорію у контейнері
WORKDIR /app

# Копіюємо файл вимог до контейнера
COPY requirements.txt ./ 

# Встановлюємо необхідні пакети з використанням pip
RUN pip install --no-cache-dir -r requirements.txt

# Копіюємо весь код проекту та дані до контейнера
COPY . .

# Якщо використовуєте дані всередині контейнера, переконайтеся, що вони скопійовані
# Приклад: COPY ./data /app/data

# Адаптація шляхів у вашому коді
# Припустимо, ваш код вже адаптовано для використання шляхів відповідно до структури контейнера

# Вказуємо команду, яка буде виконуватися при запуску контейнера
CMD ["python", "./kata.py"]
#docker run -it -v /шлях/на/вашому/комп'ютері/data:/app/data my_image_name

